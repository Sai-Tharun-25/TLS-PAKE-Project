# TLS-PAKE-Project
A simplified TLS 1.3 style handshake that integrates password authenticated key exchange. The server is authenticated using a CA signed certificate and digital signatures, while the client is authenticated using a password based mechanism during the handshake.
